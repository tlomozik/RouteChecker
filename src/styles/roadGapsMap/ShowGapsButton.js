import * as palette from '../Variables.js';
import styled from 'styled-components/native';
import {Button} from 'react-native-paper';

export const ShowGapsButton = styled(Button)`
  margin-top: 10px;
  width: 200px;
  background-color: ${palette.headerColor};
`;
